const form=document.getElementById('calculatorForm');const submitButton=form.querySelector("button[type='submit']");const resultCard=document.getElementById('resultCard');const downloadButton=document.getElementById('downloadResult');const nf=new Intl.NumberFormat('en-US');let latestResult=null,heightMode='inclusive',palletMode='preset';
function num(id){const v=Number(document.getElementById(id).value);if(!Number.isFinite(v)||v<=0)throw new Error('Enter valid values greater than 0.');return v}
function palletHeight(){if(palletMode==='custom')return num('custom_pallet_height_mm');return PalletOptimizer.PALLETS[document.getElementById('pallet_id').value].height_mm}
function payload(){const ph=palletHeight(),entered=num('max_total_height_mm');return{pallet_id:palletMode==='custom'?'custom':document.getElementById('pallet_id').value,custom_pallet_length_mm:palletMode==='custom'?num('custom_pallet_length_mm'):0,custom_pallet_width_mm:palletMode==='custom'?num('custom_pallet_width_mm'):0,custom_pallet_height_mm:palletMode==='custom'?ph:0,box_length_mm:num('box_length_mm'),box_width_mm:num('box_width_mm'),box_height_mm:num('box_height_mm'),max_total_height_mm:heightMode==='inclusive'?entered:entered+ph}}
function text(id,value){const el=document.getElementById(id);if(el)el.textContent=value}
function setPalletMode(mode){palletMode=mode;document.querySelectorAll('[data-pallet-mode]').forEach(b=>b.classList.toggle('active',b.dataset.palletMode===mode));document.getElementById('presetPalletField').hidden=mode==='custom';document.getElementById('customPalletFields').hidden=mode!=='custom'}
function setHeightMode(mode){if(mode===heightMode)return;const input=document.getElementById('max_total_height_mm'),ph=palletHeight(),value=Number(input.value)||0;input.value=mode==='exclusive'?Math.max(value-ph,1):value+ph;heightMode=mode;document.querySelectorAll('[data-height-mode]').forEach(b=>b.classList.toggle('active',b.dataset.heightMode===mode));document.getElementById('heightLabel').innerHTML=mode==='inclusive'?'Maximum pallet height <b>(including pallet)</b>':'Maximum load height <b>(excluding pallet)</b>'}
function calculate({scroll=true}={}){const error=document.getElementById('formError');error.hidden=true;submitButton.disabled=true;submitButton.textContent='Calculating…';try{latestResult=PalletOptimizer.calculate(payload());update(latestResult);downloadButton.disabled=false;if(scroll&&matchMedia('(max-width:979px)').matches)resultCard.scrollIntoView({behavior:'smooth'})}catch(e){error.textContent=e.message||'Something went wrong.';error.hidden=false}finally{submitButton.disabled=false;submitButton.textContent='▦ Calculate pallet'}}
function update(data){const cq=num('case_quantity'),load=Math.max(data.load_height_mm-data.pallet.height_mm,0);text('boxesPerLayer',nf.format(data.boxes_per_layer));text('layers',nf.format(data.layers));text('boxesPerPallet',nf.format(data.boxes_per_pallet));text('caseQuantityResult',nf.format(cq));text('palletQuantity',nf.format(data.boxes_per_pallet*cq));text('totalHeight',`${data.load_height_mm} mm`);text('palletHeight',`${data.pallet.height_mm} mm`);text('loadHeight',`${load} mm`);text('heightSummaryTotal',`${data.load_height_mm} mm`);text('resultSubtitle',`${data.pallet.name} · ${data.pallet.length_mm} × ${data.pallet.width_mm} × ${data.pallet.height_mm} mm`);text('solverStatus',data.optimality_proven?`Optimal · ${data.solver_time_ms} ms`:`Best found · ${data.solver_time_ms} ms`);const a=data.advice.minimum_reduction_for_gain;text('heightAdviceText',a?`Lower box height by ${a.reduction_mm} mm to reach ${a.new_layers} layers.`:'No practical height reduction found for an extra layer.');draw(data)}
function prep(canvas){const r=canvas.getBoundingClientRect(),w=Math.max(r.width,300),h=Math.max(r.height,260),ratio=Math.min(devicePixelRatio||1,2);canvas.width=Math.round(w*ratio);canvas.height=Math.round(h*ratio);const ctx=canvas.getContext('2d');ctx.setTransform(ratio,0,0,ratio,0,0);ctx.clearRect(0,0,w,h);return{ctx,w,h}}
function poly(ctx,pts,fill,stroke='rgba(84,55,29,.48)'){ctx.beginPath();ctx.moveTo(pts[0].x,pts[0].y);pts.slice(1).forEach(p=>ctx.lineTo(p.x,p.y));ctx.closePath();ctx.fillStyle=fill;ctx.fill();ctx.strokeStyle=stroke;ctx.lineWidth=.75;ctx.stroke()}
function draw(data){const canvas=document.getElementById('palletCanvas'),{ctx,w,h}=prep(canvas),p=data.pallet,total=Math.max(data.load_height_mm,p.height_mm+data.input.box_height_mm),raw=(x,y,z)=>({x:x-y,y:-(x+y)*.34-z*.62}),bounds=[];[0,p.length_mm].forEach(x=>[0,p.width_mm].forEach(y=>[0,total].forEach(z=>bounds.push(raw(x,y,z)))));const minX=Math.min(...bounds.map(q=>q.x)),maxX=Math.max(...bounds.map(q=>q.x)),minY=Math.min(...bounds.map(q=>q.y)),maxY=Math.max(...bounds.map(q=>q.y)),scale=Math.min((w-70)/(maxX-minX),(h-30)/(maxY-minY)),origin={x:35+(w-70-(maxX-minX)*scale)/2-minX*scale,y:15+(h-30-(maxY-minY)*scale)/2-minY*scale},pr=(x,y,z)=>{const q=raw(x,y,z);return{x:origin.x+q.x*scale,y:origin.y+q.y*scale}};function cube(x,y,z,l,d,bh,c){const a=pr(x,y,z),b=pr(x+l,y,z),dd=pr(x,y+d,z),at=pr(x,y,z+bh),bt=pr(x+l,y,z+bh),ct=pr(x+l,y+d,z+bh),dt=pr(x,y+d,z+bh);poly(ctx,[a,dd,dt,at],c.left);poly(ctx,[a,b,bt,at],c.right);poly(ctx,[at,bt,ct,dt],c.top)}ctx.strokeStyle='rgba(148,163,184,.12)';for(let i=-4;i<=4;i++){let a=pr(i*230,-450,0),b=pr(i*230,1300,0);ctx.beginPath();ctx.moveTo(a.x,a.y);ctx.lineTo(b.x,b.y);ctx.stroke()}cube(0,0,0,p.length_mm,p.width_mm,p.height_mm,{top:'#d5a468',left:'#9b6536',right:'#b97d45'});const sorted=[...data.layout].sort((a,b)=>(b.x_mm+b.y_mm)-(a.x_mm+a.y_mm));for(let layer=0;layer<data.layers;layer++){const z=p.height_mm+layer*data.input.box_height_mm;sorted.forEach(box=>cube(box.x_mm,box.y_mm,z,box.length_mm,box.width_mm,data.input.box_height_mm,{top:box.rotated?'#d8a15c':'#e0ad68',left:box.rotated?'#a76831':'#b47236',right:box.rotated?'#bd7b3e':'#c88748'}))}}
function download(){if(!latestResult)return;const src=document.getElementById('palletCanvas'),c=document.createElement('canvas');c.width=1600;c.height=1000;const x=c.getContext('2d');x.fillStyle='#f6f8fc';x.fillRect(0,0,1600,1000);x.fillStyle='#2563eb';x.fillRect(0,0,1600,90);x.fillStyle='#fff';x.font='800 34px Arial';x.fillText('Pallet Optimizer · Result',55,57);x.fillStyle='#0f172a';x.font='700 24px Arial';x.fillText(`${latestResult.boxes_per_layer} per layer · ${latestResult.layers} layers · ${latestResult.boxes_per_pallet} boxes`,55,145);x.drawImage(src,120,190,1360,720);c.toBlob(blob=>{const u=URL.createObjectURL(blob),a=document.createElement('a');a.href=u;a.download=`box_${num('box_length_mm')}x${num('box_width_mm')}x${num('box_height_mm')}mm.png`;a.click();URL.revokeObjectURL(u)},'image/png')}
document.querySelectorAll('[data-pallet-mode]').forEach(b=>b.onclick=()=>setPalletMode(b.dataset.palletMode));document.querySelectorAll('[data-height-mode]').forEach(b=>b.onclick=()=>setHeightMode(b.dataset.heightMode));form.onsubmit=e=>{e.preventDefault();calculate()};downloadButton.onclick=download;addEventListener('resize',()=>latestResult&&draw(latestResult));if('serviceWorker'in navigator)navigator.serviceWorker.register('service-worker.js').catch(()=>{});calculate({scroll:false});